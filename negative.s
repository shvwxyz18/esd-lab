	AREA NEGATIVE, CODE, READONLY
	EXPORT __main

__main
	LDR R0, =ARRAY
	MOV R2, #4
	MOV R3, #0
	
LOOP 
	LDR R1, [R0], #4
	CMP R1, #0
	BLT COUNT
	B LOOP2
	
COUNT
	ADD R3, #1
	
LOOP2
	SUBS R2, #1
	CMP R2, #1
	BNE LOOP
	LDR R6, =RESULT
	STR R3, [R6]

STOP B STOP
ARRAY DCD 0XFFFFFFF1, 0X11111111, 0XFFFFFFF6, 0X01
	AREA RES, DATA, READWRITE
RESULT DCD 0X00

 ;EXPECTED RESULT R3 -> 0X00000002
